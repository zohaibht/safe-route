import { AppRegistry } from 'react-native';
import App from './App';

// The name must match the 'name' in your app.json and native config (MainActivity.java/AppDelegate.m)
const appName = 'MyReactNativeApp';

AppRegistry.registerComponent(appName, () => App);